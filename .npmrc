# Use legacy peer deps resolution to avoid conflicts
legacy-peer-deps=true

# Disable audit to speed up installation
audit=false

# Disable fund messages
fund=false

# Prefer offline mode when packages are in cache
prefer-offline=true

# Set cache location explicitly for Railway
cache=/tmp/.npm
